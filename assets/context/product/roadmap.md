---
ctx:
  tags: [product]
  purpose: Capture what is in progress, next, and later so AI does not propose work that conflicts with planned direction.
  use_when:
    - checking near-term priorities
    - evaluating whether a suggestion conflicts with known upcoming work
  fill:
    - required: now
    - required: next
    - optional: later
    - optional: dependencies and milestones
---

# Roadmap

> Project-specific. Keep this short and current. A stale roadmap is worse than no roadmap.
