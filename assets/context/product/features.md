---
ctx:
  tags: [product]
  purpose: Map user-facing capabilities to their current status and implementation areas.
  use_when:
    - understanding what the product already does
    - locating code related to a feature area
  fill:
    - required: feature name
    - required: what it does
    - required: status
    - optional: key modules, services, or files
---

# Features

> Project-specific. List the important product features, not every internal implementation detail.
