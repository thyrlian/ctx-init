---
ctx:
  tags: [standards]
  purpose: Define the naming rules that generated and hand-written artifacts should follow in this project.
  use_when:
    - creating or reviewing identifiers, files, routes, schemas, or events
    - checking whether new code matches existing naming conventions
  fill:
    - required: code and file naming rules
    - required: API or schema naming rules
    - optional: database, queue, and job naming rules
---

# Naming Conventions

> Project-specific. Keep this concrete. Real examples are more useful than generic style advice.
>
> If the project has dedicated naming rules for analytics events, page views,
> screen views, or tracking properties, document those in `analytics.md` and
> reference them here only if needed.
